import { Identifiers } from '@angular/compiler';


export class Column   {
    constructor(
        public arr?: any[""], 
        public counter?: number,
        public id?:number
    ) {
    }
   
}